// Importing the createContext from react module
import {createContext} from 'react'

// creating a context which will hold all the states regarding notes
const NoteContext = createContext();

export default NoteContext;